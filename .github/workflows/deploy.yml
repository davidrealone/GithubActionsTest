name: CI/CD Pipeline
on:
  push:
    branches: [main]

jobs:
  build-test:
    name: Build and Test
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          pnpm install
          echo "依赖安装完成"

      - name: Run tests
        run: pnpm run test:unit
        env:
          CI: true

      - name: Build project
        run: |
          pnpm run build
          echo "构建完成，产物大小："
          du -sh dist/ || true
          echo "dist目录内容："
          ls -la dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: |
            dist/
            !dist/**/*.map
          retention-days: 7

  deploy:
    name: Deploy to Production
    needs: build-test
    runs-on: self-hosted
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: production-build

      - name: Verify build artifacts
        run: |
          echo "验证构建产物..."
          if [ ! -d "./dist" ]; then
            echo "❌ 错误: dist目录不存在"
            exit 1
          fi

          echo "dist目录内容:"
          ls -la dist/
          echo "HTML文件: $(find dist/ -name '*.html' | wc -l)个"
          echo "JS文件: $(find dist/ -name '*.js' | wc -l)个"
          echo "CSS文件: $(find dist/ -name '*.css' | wc -l)个"

      - name: Setup SSH Connection
        run: |
          echo "=== 配置SSH连接 ==="

          CURRENT_USER=$(whoami)
          echo "当前用户: $CURRENT_USER"

          if [ "$CURRENT_USER" = "root" ]; then
            export HOME=/root
          else
            export HOME=/home/$CURRENT_USER
          fi

          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          rm -f ~/.ssh/id_ed25519 ~/.ssh/id_ed25519.pub ~/.ssh/config

          cp /home/github-runner/.ssh/id_ed25519 ~/.ssh/
          cp /home/github-runner/.ssh/id_ed25519.pub ~/.ssh/
          chmod 600 ~/.ssh/id_ed25519
          chmod 644 ~/.ssh/id_ed25519.pub

          cat > ~/.ssh/config << 'EOF'
          Host 192.168.1.78
            HostName 192.168.1.78
            User fuint
            IdentityFile ~/.ssh/id_ed25519
            StrictHostKeyChecking no
            ConnectTimeout 15
          EOF

          chmod 600 ~/.ssh/config

      - name: Test SSH Connection
        run: |
          echo "测试SSH连接..."
          if ssh -o ConnectTimeout=10 192.168.1.78 "echo '✅ 连接成功'"; then
            echo "✅ SSH连接正常"
          else
            echo "❌ SSH连接失败"
            exit 1
          fi

      - name: Deploy dist directory
        env:
          DEPLOY_PATH: /home/fuint/frontsite
        run: |
          echo "=== 部署dist目录 ==="
          echo "目标: $DEPLOY_PATH"

          # 检查目标目录是否存在，不存在则创建
          ssh 192.168.1.78 "mkdir -p $DEPLOY_PATH"

          echo "开始同步dist目录..."
          rsync -avz --progress --delete \
            --exclude='*.map' \
            dist/ \
            fuint@192.168.1.78:$DEPLOY_PATH/

          echo "✅ 部署完成"

      - name: Verify deployment
        run: |
          echo "=== 部署验证 ==="

          ssh 192.168.1.78 "
            echo '目标服务器验证:'
            echo '================'
            echo '目录: /home/fuint/frontsite'
            echo '大小: \$(du -sh /home/fuint/frontsite)'
            echo '文件数: \$(find /home/fuint/frontsite -type f | wc -l)'
            echo ''
            echo '关键文件检查:'
            [ -f '/home/fuint/frontsite/index.html' ] && echo '✅ index.html 存在' || echo '❌ index.html 缺失'
            [ -f '/home/fuint/frontsite/favicon.ico' ] && echo '✅ favicon.ico 存在' || echo '⚠️  favicon.ico 缺失'
            [ -f '/home/fuint/frontsite/main.js' ] && echo '✅ main.js 存在' || echo '⚠️  main.js 缺失'
            [ -f '/home/fuint/frontsite/main.css' ] && echo '✅ main.css 存在' || echo '⚠️  main.css 缺失'
          "

          echo "✅ 验证完成"

      - name: Deployment success
        run: |
          echo "🎉 部署成功！"
          echo "时间: $(date)"
          echo "Commit: ${{ github.sha }}"
